apiVersion: v1
kind: ConfigMap
metadata:
  name: catalogue
  namespace: roboshop
  labels:
    component: catalogue
    project: roboshop
    env: dev
data:
  MONGO: "true" 
  MONGO_URL: {{ .Values.configMap.MONGO_URL }}