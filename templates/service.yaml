apiVersion: v1
kind: Service
metadata:
  name: catalogue
  namespace: roboshop
  labels:
    component: catalogue
    project: roboshop
    env: dev
spec:
  selector:
    component: catalogue
    project: roboshop
    env: dev
  ports:
   - protocol: TCP
     port: 8080
     targetPort: {{.Values.service.targetPort}}
 